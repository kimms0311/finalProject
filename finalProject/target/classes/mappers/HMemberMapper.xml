<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.avo.www.repository.HmemberDAO">
	<select id="getDeail" resultType="com.avo.www.security.MemberVO">
		select * from member where mem_email = #{email}
	</select>
	<select id="getPw" resultType="String">
		select mem_pw from member where mem_email = #{email}
	</select>
	<update id="modifyPwEmpty">
		update member set mem_nick_name = #{memNickName}, mem_sido = #{memSido}, mem_sigg = #{memSigg}, mem_emd = #{memEmd}, mem_mod_at = now() where mem_email = #{memEmail}
  	</update>
  	<update id="modify">
  		update member set mem_pw = #{memPw}, mem_nick_name = #{memNickName}, mem_sido = #{memSido}, mem_sigg = #{memSigg}, mem_emd = #{memEmd}, mem_mod_at = now() where mem_email = #{memEmail}
  	</update>	
  	<update id="jjsDelete">
  		update product set pro_is_del = 'Y' where pro_email = #{email}
  	</update>
  	<update id="cmDelete">
  		update community set cm_is_del = 'Y' where cm_email = #{email}
  	</update>
  	<delete id="amDelte">  		
  		delete from auth_member where email = #{email}
  	</delete>
  	<delete id="mbDelete">
  		delete from member where mem_email = #{email}
  	</delete>
</mapper>
